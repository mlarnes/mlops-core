apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow-pvc
  namespace: mlops
  labels:
    app: mlflow-server
    component: mlflow
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Laisse le StorageClass par défaut du cluster (ex: standard sur minikube)

